# user data input
import math

r = float(input())

# logic
calculated_area = math.pi * (r * r)
calculated_parameter = math.pi * (2 * r)
# result
print(f'{calculated_area:.2f}')
print(f'{calculated_parameter:.2f}')
